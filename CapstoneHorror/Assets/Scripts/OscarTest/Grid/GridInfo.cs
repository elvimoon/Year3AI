using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//reference used: https://www.youtube.com/watch?v=fUiNDDcU_I4
//this script will check the info and stats of the grid, which GridBehavior will require

public class GridInfo : MonoBehaviour
{
    //x and y corresponds to the coordinates of nodes in grid
    public int visitedNode = -1;
    public int x = 0;
    public int y = 0;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
